# Tetris
A simple implementation of tetris.

## Testing
To run tests:
```shell
chmod +x run_tests
./run_tests
```

## Use:
To produce outputs:
```shell
chmod +x tetris
./tetris < input.txt > output.txt
```